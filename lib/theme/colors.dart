import 'package:flutter/material.dart';

class AppColors {
  static const app = Color(0xff9c1b2f);
  static const switchButton = Color(0xffaa2d40);
  static const mode = Color(0xffc81727);
  static const text = Color(0xff2c343a);
  static const muon = Color(0xff4169E1);
  static const gradient = LinearGradient(
    begin: Alignment.topRight,
    end: Alignment.bottomLeft,
    colors: [
      Colors.blue,
      Colors.red,
    ],
  );
}

